// Stub. T4 replaces this body with the project editor, serving both
// /admin/projects/new and /admin/projects/:id/edit. Both routes are already
// wired in App.jsx, outside the admin split shell, and every string it needs is
// in both catalogues under `admin.projects.*` (plus the existing `form.*` block
// that PostForm reads).
export default function AdminProjectEdit() {
  return <p>AdminProjectEdit</p>;
}
