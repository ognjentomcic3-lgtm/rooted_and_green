// Stub. T3 replaces this body with the projects pane (the list, the featured
// checkboxes and the New project button). The route is already wired in
// App.jsx and every string it needs is in both catalogues under
// `admin.projects.*` and `admin.featured.*`.
export default function AdminProjects() {
  return <p>AdminProjects</p>;
}
