// Stub. T5 replaces this body with the references pane (the nested split: the
// list on the left, ReferenceForm on the right). The route is already wired in
// App.jsx and every string it needs is in both catalogues under
// `admin.references.*`, `admin.featured.*` and `referenceForm.*`.
export default function AdminReferences() {
  return <p>AdminReferences</p>;
}
